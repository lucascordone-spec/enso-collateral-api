{
  "name": "enso-collateral-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:mcp": "npm run build --workspace=@enso-collateral/mcp-server",
    "start:mcp": "npm run start --workspace=@enso-collateral/mcp-server"
  }
}
